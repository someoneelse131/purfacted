// PurFacted 2.0 - Prisma schema
// Core domain models are added in R2 (see REQUIREMENTS.md).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
